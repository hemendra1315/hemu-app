# Stitch Prompt — 09 · Members (Future)

**Screen:** Members
**Status:** ⏭️ **Not generated** — future / planned screen
**Design viewport:** 390px mobile

## Status
This scaffold is **not** an approved artifact. It is recorded here so the prompt language and design intent are preserved for a future pass, but **no new Stitch screen is to be generated until the review is approved**.

## Purpose
Academy member management — invite + role management (coach invitations, member role changes, join-code entry). This maps to `src/features/members` in the React app.

## Intended design language (not yet applied)
- Floodlit Turf + Scorebook system (see `cricket-academy-stitch-design-system.md`, asset `a4001a0faef843968679f0b6bccf4485`).
- Mobile-first on a 390px viewport; quiet Surface cards, 12px radius, 1px `Line` borders; mono identifiers; saffron for key status only.

## Material to be produced when approved
1. Member list (roster + roles).
2. Invite / join-code entry flow.
3. Role-change / permission states.

## Guardrails
- **Do not generate this screen now.**
- Requires fresh review + approval + a new Stitch pass before any visual work proceeds.

---

*Placeholder only. Not an approved design.*